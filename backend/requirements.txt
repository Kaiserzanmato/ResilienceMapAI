fastapi==0.141.1
starlette==1.6.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.11.0
pydantic_core==2.46.4
python-dotenv==1.2.2
pytest==9.1.1
pytest-asyncio==1.4.0
reportlab==4.5.1
uvicorn==0.39.0

# Persistence (optional — only used when DATABASE_URL is set; the app runs
# fully on in-memory stores otherwise). Migrations run out-of-band via Alembic,
# never inside the serverless function.
# sqlalchemy must be >=2.0.37 — 2.0.36's declarative-mapping code (used by
# the Mapped[...] type hints in app/models.py) crashes on Python 3.14+ with
# "TypeError: descriptor '__getitem__' requires a 'typing.Union' object but
# received a 'tuple'". Confirmed broken on 2.0.36, confirmed fixed on 2.0.51
# (2026-08-01, against Python 3.14.6).
sqlalchemy[asyncio]==2.0.51
asyncpg==0.30.0
alembic==1.14.0

# Firecrawl advisory scraper (optional — only used when FIRECRAWL_API_KEY is
# set; see app/data_sources/scrapers/firecrawl_worker.py).
firecrawl-py==4.34.0
