# MCP Assessment

No MCP integration is recommended. Scheduled ingestion, validation, caching and map APIs are deterministic direct-API work and MCP adds no capability while expanding tool, credential, prompt-injection, availability and audit surfaces. If reconsidered, require an official server, allowlisted read-only tools, argument validation, timeouts, isolated credentials and invocation logging. Verdict: REJECT.
